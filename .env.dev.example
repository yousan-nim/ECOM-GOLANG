API_TAG=dev

# Databases (one per service)
CATALOG_DB=catalog_db
CATALOG_DB_USER=catalog
CATALOG_DB_PASSWORD=changeme-dev

ORDER_DB=order_db
ORDER_DB_USER=orderuser
ORDER_DB_PASSWORD=changeme-dev

PAYMENT_DB=payment_db
PAYMENT_DB_USER=paymentuser
PAYMENT_DB_PASSWORD=changeme-dev

# Auth
JWT_SECRET=changeme-dev-secret

# pgAdmin
PGADMIN_EMAIL=admin@ecom.dev
PGADMIN_PASSWORD=changeme
